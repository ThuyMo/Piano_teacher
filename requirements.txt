# Web framework
fastapi==0.136.3
uvicorn==0.49.0
python-multipart==0.0.32

# MIDI
mido==1.3.3

# Music analysis
music21==10.3.0

# Data
pandas==3.0.3

# Validation (pulled in by fastapi, pinned for reproducibility)
pydantic==2.13.4

# Piano transcription — also requires ffmpeg installed on the system
# Install ffmpeg: brew install ffmpeg  (macOS)  |  apt install ffmpeg  (Linux)
transkun==2.0.1
